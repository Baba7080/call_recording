# Copy this file to .env and update it with your Atlas credentials.
# You can either provide a full encoded URI or raw credentials below.

# Option 1: Full URI (must encode special characters in password)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.kursdso.mongodb.net/caall_db?authSource=admin&retryWrites=true&w=majority

# Option 2: Separate env vars (recommended when password contains @)
MONGODB_USERNAME=abhishekshukla0700
MONGODB_PASSWORD=Abhi6394@
MONGODB_HOST=cluster0.kursdso.mongodb.net
MONGODB_DATABASE=caall_db
MONGODB_OPTIONS=retryWrites=true&w=majority
